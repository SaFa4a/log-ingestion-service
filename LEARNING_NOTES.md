# ملاحظات التعلّم: افهم المشروع من الصفر

## المرحلة 1: يعني شو "Logs"؟ (اشرحلي وكأني بعمر 10 سنين)

تخيّل عندك **حارس عمارة** يكتب دفتر ملاحظات: "الساعة 2:15 دخل رجل للمصعد"،
"الساعة 2:17 المصعد علق بالطابق 3"، "الساعة 2:20 تم إصلاح المصعد".
هذا بالضبط الـ **Log**: جملة قصيرة، مع وقت، تقول *شو صار* بجزء معيّن من النظام.

كل تطبيق (تسجيل دخول، دفع، شحن...) هو "طابق" بالعمارة، وكل طابق عنده حارسه
الخاص يكتب "صار خطأ بالدفع"، "المستخدم 42 سجّل دخول"، إلخ. لما يصير عندك
مئات الطوابق (Services) وملايين السطور باليوم، ما بتقدر تفتح كل دفتر
لحالو وتدوّر — هون بيجي دور Datadog / Loki: نظام مركزي يجمع كل الدفاتر،
ويخليك تدوّر وتسأل بثانية وحدة: "ورّيني كل أخطاء الدفع بالساعة الأخيرة".

### الإدخال الضخم (Ingestion) — تشبيه مكتب البريد
تخيّل مكتب بريد بيوصله 15,000 رسالة **بالثانية**. لو الموظف بده يفتح كل
ظرف ويقرأه ويرتبه لحاله، بينهار بالثانية الأولى. الحل: يجمع كومة رسائل
سوا (Batch)، ويحطها دفعة وحدة بالخزنة الصحيحة بحركة وحدة، مش حركة لكل
رسالة. هاد بالضبط شو بيعمل الـ `unnest`-based bulk insert بمشروعنا —
بدل ما نعمل INSERT لكل سطر، منجمعهم كلهم ومنرسلهم لقاعدة البيانات
بطلب SQL واحد.

### الاستعلام والتجميع (Query & Aggregation) — تشبيه خزانة الألعاب
تخيّل خزانة ألعاب ضخمة، فيها آلاف الصناديق. لو رتبتها بس "بالترتيب اللي
وصلت فيه" (زي كومة عشوائية)، لازم تفتش كل صندوق لتلاقي لعبة معيّنة.
لكن لو رتبتها بفهرس ("كل صناديق السيارات هون، كل صناديق الدمى هونيك")،
بتلاقي اللي بدك ياه بثواني. هاد هو دور الـ **Indexes**: بدل ما نفتش
كل الجدول (Sequential Scan)، منستخدم فهرس جاهز يوديك مباشرة للمكان
الصح.

### سياسة الحذف (Retention) — تشبيه دفتر يومي جديد كل يوم
تخيّل الحارس ما بيستخدم دفتر واحد طول عمره — كل يوم بفتح دفتر جديد.
لما يعدي شهر، بدل ما يقعد يمحي سطر سطر من الدفاتر القديمة (تعب ووقت
ضايع)، بيمزّق الدفتر كامل ويرميه. هاد بالضبط الـ **Partitioning**:
كل يوم إله "دفتر" (Partition) لحاله، وحذف يوم قديم = رمي الدفتر
كامل (`DROP TABLE`) — عملية سريعة جداً، مش حذف صف صف.

### الموارد المحدودة (0.5 CPU / 256MB للتطبيق، 1 CPU / 1GB لقاعدة البيانات)
هاد يعني إنو ما فيك "ترمي مشكلة بالـ Hardware". لو عندك سيرفر ضخم،
فيك تغطي أخطاء التصميم بقوة المعالج. هون العكس: لازم كل قرار (نوع
الفهرس، حجم الـ Connection Pool، طريقة الإدخال) يكون **مقصود ومحسوب**،
لأنو مافي هامش يسمح بالهدر. مثلاً: Connection Pool كبير على معالج
وحدة بس بيزيد الـ Context Switching بدون ما يزيد الأداء الفعلي —
لهيك اخترنا Pool صغير (8 اتصالات) مع Batch Insert بدل اتصالات كتير
متوازية.

---

## المرحلة 2: كتالوج القرارات الهندسية (Why X not Y)

| القرار | اخترنا | ليش مش البديل |
|---|---|---|
| **تخزين الـ Attributes** | JSONB عمود واحد | **EAV Table**: كل log بيتحول لعدة صفوف (صف لكل مفتاح) — هاد بيضاعف الكتابة بالضبط بالمكان اللي بدنا فيه سرعة (15k/sec). **Text Column**: ما فيك تعمل فلترة أو فهرسة عليها أبداً، بس تخزين عمي. JSONB بيعطيك صف واحد لكل log + إمكانية فهرسة GIN لاحقاً، وما بتحتاج migration كل ما ظهر مفتاح جديد. |
| **سياسة الحذف (Retention)** | Range Partitioning + DROP TABLE | `DELETE FROM logs WHERE ts < X`: بيمسح صف صف، بيولّد WAL بقد عدد الصفوف، وبيسيب "فراغات" (Dead Tuples) لازم VACUUM يلحقهم — كل هاد عبء إضافي عالمعالج الوحيد اللي عنا. حذف Partition كامل هو عملية Metadata بس، بتاخد أجزاء الثانية. |
| **الترقيم (Pagination)** | Cursor-based (Keyset) | `OFFSET/LIMIT`: لو طلبت صفحة 500 بحجم 100، قاعدة البيانات لازم تقرا وترمي أول 50,000 صف قبل ما ترجعلك النتيجة — كلفة بتزيد كل ما تعمّقت بالصفحات. الـ Cursor منقلّه "آخر (ts, id) شفتهم"، وبيروح مباشرة للفهرس من هالنقطة — كلفة ثابتة بغض النظر عن عمق الصفحة. |
| **عميل قاعدة البيانات** | Raw SQL عبر `pg` مباشرة | ORM ثقيل (Prisma/TypeORM): بيضيف طبقة تفسير (query building, hydration) بتاكل معالج وذاكرة احنا ما معنا فايض منهم (256MB!). كمان أغلب الـ ORMs ما بتدعم كتابة SQL محسّن يدوياً بسهولة زي `unnest` bulk insert أو partition-aware queries. |
| **الفهارس (Indexes)** | B-Tree مركّب (service/level + ts DESC + id) + GIN Trigram على message | فهرس B-Tree بسيط على عمود واحد ما بيغطي نمط الاستعلام الفعلي (فلترة + ترتيب زمني سوا). أما الـ Trigram (pg_trgm) فهو الوحيد اللي بيقدر يسرّع بحث `ILIKE '%نص%'` (substring من النص من أي مكان) — B-Tree عادي ما بيقدر يخدم بحث بيبدأ بـ `%`. |

### سؤال متوقع بالمقابلة: "ليش الـ GIN index على attributes ما عم يسرّع فلتر المساواة؟"
جواب صادق ومباشر: لأنو GIN على JSONB بيسرّع عمليات الاحتواء (`@>`) أو
وجود مفتاح (`?`)، مش استخراج نص (`->>`) بمقارنة مساواة على مفتاح
عشوائي. العقد (Contract) بيطلب "compared as strings" يعني لازم نستخدم
`->>` بالضبط. هاد trade-off واعي ومكتوب بالـ README تحت "Known
Limitations" — والحل المؤقت إنو منصغّر الجدول أول بفلاتر service/
level/time (كلهم مفهرسين) قبل ما نطبق فلتر الـ attribute.
